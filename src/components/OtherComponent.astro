<!-- Hero/About Section -->
<section class="hero">
  <div class="hero-container">
    <div class="hero-content">
      <div class="hero-text">
        <br />
        <br />
        <br />
        <span class="typing-text fade-in delay-1"
          >About ProtectQube<span class="peace">
            <svg
              width="32"
              height="32"
              viewBox="0 0 24 24"
              fill="none"
              xmlns="http://www.w3.org/2000/svg"
            >
              <rect
                x="3"
                y="11"
                width="18"
                height="11"
                rx="2"
                ry="2"
                stroke="currentColor"
                stroke-width="2"></rect>
              <circle cx="12" cy="16" r="1" fill="currentColor"></circle>
              <path d="M7 11V7A5 5 0 0 1 17 7V11" stroke="currentColor" stroke-width="2"></path>
            </svg>
          </span></span
        >

        <p class="fade-in delay-2">
          ProtectQube was developed based on our extensive experience in cybersecurity and IoT
          protection. We understand the critical security challenges faced by financial institutions
          in protecting ATM networks from sophisticated cyber threats and physical tampering.
        </p>

        <div class="cta-buttons-left fade-in delay-3">
          <a href="#features" class="btn-primary">Explore Security Features</a>
          <a href="/contact" class="btn-secondary">Secure Your ATMs</a>
        </div>
      </div>

      <div class="hero-image fade-in delay-4">
        <img
          src="/assets/img/pq-6.png"
          alt="ATM Security Dashboard"
          srcset=""
        />
      </div>
    </div>
  </div>
</section>

<!-- Features Section -->
<section class="features" id="features">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Why Choose ProtectQube?</h2>
      <p class="section-subtitle">
        Comprehensive IoT security solution protecting your ATMs anywhere, anytime
      </p>
    </div>

    <div class="features-grid">
      <div class="feature-card fade-in">
        <div class="feature-icon">
          <svg
            width="48"
            height="48"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M12 22C17.5228 22 22 17.5228 22 12C22 6.47715 17.5228 2 12 2C6.47715 2 2 6.47715 2 12C2 17.5228 6.47715 22 12 22Z"
              stroke="currentColor"
              stroke-width="2"></path>
            <path
              d="M9 12L11 14L15 10"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"></path>
          </svg>
        </div>
        <h3>Advanced Threat Detection</h3>
        <p>
          ProtectQube provides AI-powered threat detection that monitors your ATM network 24/7,
          identifying and blocking sophisticated cyber attacks before they can compromise your
          systems.
        </p>
      </div>

      <div class="feature-card fade-in">
        <div class="feature-icon">
          <svg
            width="48"
            height="48"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2"></circle>
            <path d="M2 12H22" stroke="currentColor" stroke-width="2"></path>
            <path
              d="M12 2A15.3 15.3 0 0 1 16 12A15.3 15.3 0 0 1 12 22A15.3 15.3 0 0 1 8 12A15.3 15.3 0 0 1 12 2Z"
              stroke="currentColor"
              stroke-width="2"></path>
          </svg>
        </div>
        <h3>Secure Anywhere Coverage</h3>
        <p>
          With global IoT connectivity, ProtectQube secures your ATMs regardless of location,
          providing consistent protection across urban centers and remote deployments.
        </p>
      </div>

      <div class="feature-card fade-in">
        <div class="feature-icon">
          <svg
            width="48"
            height="48"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2"></circle>
            <polyline
              points="12,6 12,12 16,14"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"></polyline>
          </svg>
        </div>
        <h3>24/7 Real-time Monitoring</h3>
        <p>
          Continuous monitoring and instant alerts ensure your ATM network is protected around the
          clock, with immediate response to any security incidents or anomalies.
        </p>
      </div>

      <div class="feature-card fade-in">
        <div class="feature-icon">
          <svg
            width="48"
            height="48"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <rect
              x="3"
              y="11"
              width="18"
              height="11"
              rx="2"
              ry="2"
              stroke="currentColor"
              stroke-width="2"></rect>
            <circle cx="12" cy="16" r="1" fill="currentColor"></circle>
            <path d="M7 11V7A5 5 0 0 1 17 7V11" stroke="currentColor" stroke-width="2"></path>
          </svg>
        </div>
        <h3>Multi-Layer Security</h3>
        <p>
          Comprehensive protection combining network security, device authentication, data
          encryption, and physical tampering detection for complete ATM security coverage.
        </p>
      </div>

      <div class="feature-card fade-in">
        <div class="feature-icon">
          <svg
            width="48"
            height="48"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M3 3V21H21V9L15 3H3Z"
              stroke="currentColor"
              stroke-width="2"
              stroke-linejoin="round"></path>
            <path d="M9 9H15" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path>
            <path d="M9 13H15" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path>
            <path d="M9 17H12" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path>
          </svg>
        </div>
        <h3>Security Analytics Dashboard</h3>
        <p>
          Centralized security operations center with real-time threat intelligence, incident
          tracking, and comprehensive reporting for informed security decision-making.
        </p>
      </div>

      <div class="feature-card fade-in">
        <div class="feature-icon">
          <svg
            width="48"
            height="48"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M20 6L9 17L4 12"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"></path>
            <circle cx="12" cy="12" r="3" stroke="currentColor" stroke-width="2"></circle>
            <path d="M12 1V3" stroke="currentColor" stroke-width="2"></path>
            <path d="M12 21V23" stroke="currentColor" stroke-width="2"></path>
            <path d="M4.22 4.22L5.64 5.64" stroke="currentColor" stroke-width="2"></path>
            <path d="M18.36 18.36L19.78 19.78" stroke="currentColor" stroke-width="2"></path>
            <path d="M1 12H3" stroke="currentColor" stroke-width="2"></path>
            <path d="M21 12H23" stroke="currentColor" stroke-width="2"></path>
            <path d="M4.22 19.78L5.64 18.36" stroke="currentColor" stroke-width="2"></path>
            <path d="M18.36 5.64L19.78 4.22" stroke="currentColor" stroke-width="2"></path>
          </svg>
        </div>
        <h3>Financial Security Partner</h3>
        <p>
          ProtectQube is more than just a security tool—it's your trusted partner in maintaining the
          highest standards of financial service security and regulatory compliance.
        </p>
      </div>
    </div>
  </div>
</section>

<!-- Benefits Section -->
<section class="benefits">
  <div class="container">
    <div class="benefits-content">
      <div class="benefits-text">
        <h2>Secure Your ATM Network</h2>
        <p>
          ProtectQube empowers financial institutions to achieve unparalleled security standards
          through cutting-edge IoT protection and intelligent threat prevention.
        </p>

        <div class="benefits-list">
          <div class="benefit-item">
            <span class="benefit-icon">✓</span>
            <div>
              <h4>Zero-Trust Security</h4>
              <p>
                Implement zero-trust architecture with continuous verification and device
                authentication for maximum ATM network protection.
              </p>
            </div>
          </div>

          <div class="benefit-item">
            <span class="benefit-icon">✓</span>
            <div>
              <h4>Fraud Prevention</h4>
              <p>
                Advanced fraud detection algorithms and behavioral analysis prevent skimming, card
                trapping, and other ATM-specific attacks.
              </p>
            </div>
          </div>

          <div class="benefit-item">
            <span class="benefit-icon">✓</span>
            <div>
              <h4>Compliance Assurance</h4>
              <p>
                Meet PCI DSS, ISO 27001, and banking regulatory requirements with automated
                compliance monitoring and reporting capabilities.
              </p>
            </div>
          </div>
        </div>
      </div>

      <div class="benefits-visual">
        <div class="stats-grid">
          <div class="stat-card">
            <h3>99.9%</h3>
            <p>Threat Detection Rate</p>
          </div>
          <div class="stat-card">
            <h3>24/7</h3>
            <p>Security Monitoring</p>
          </div>
          <div class="stat-card">
            <h3>< 1s</h3>
            <p>Response Time</p>
          </div>
          <div class="stat-card">
            <h3>100%</h3>
            <p>Compliance Ready</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CTA Section -->
<section class="cta-section">
  <div class="container">
    <div class="cta-content">
      <h2>Ready to Secure Your ATM Network?</h2>
      <p>
        Join leading financial institutions already using ProtectQube to protect their ATM
        infrastructure with next-generation IoT security solutions.
      </p>
      <div class="cta-buttons">
        <a href="/contact" class="btn-secondary large">Request Security Assessment</a>
      </div>
    </div>
  </div>
</section>

<style>
  /* Hero Section */
  .hero {
    background: var(--container-color);
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 3rem 1rem;
    position: relative;
    overflow: hidden;
  }

  .hero-container {
    max-width: 1200px;
    width: 100%;
    margin: 0 auto;
    color: var(--text-header);
    position: relative;
    z-index: 1;
  }

  .hero-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 3rem;
  }

  .hero-text {
    flex: 1;
    text-align: left;
  }

  .hero-image {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .typing-text {
    font-weight: bold;
    font-size: 3rem;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
    display: block;
    margin-bottom: 1.5rem;
    color: var(--heading-color);
  }

  .peace {
    display: inline-block;
    transition: transform 0.3s ease;
    color: var(--primary);
  }

  .peace svg {
    width: 32px;
    height: 32px;
    vertical-align: middle;
    margin-left: 0.5rem;
  }

  .typing-text:hover .peace {
    transform: rotate(15deg);
  }

  .hero p {
    font-size: 1.2rem;
    max-width: 600px;
    margin: 0 0 2rem 0;
    color: var(--text-color);
    line-height: 1.6;
  }

  /* Buttons */
  .cta-buttons {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
    margin-top: 2rem;
  }

  .cta-buttons-left {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: flex-start;
    margin-top: 2rem;
  }

  .btn-primary,
  .btn-secondary {
    padding: 1rem 2rem;
    border-radius: var(--border-radius);
    text-decoration: none;
    font-weight: 600;
    transition: var(--transition);
    display: inline-block;
  }

  .btn-primary {
    background: var(--primary);
    color: white;
    border: 2px solid var(--primary);
  }

  .btn-primary:hover {
    background: var(--primary-dark);
    transform: translateY(-2px);
  }

  .btn-secondary {
    background: transparent;
    color: var(--heading-color);
    border: 2px solid var(--heading-color);
  }

  .btn-secondary:hover {
    background: var(--heading-color);
    color: var(--bg-color);
  }

  .btn-primary.large,
  .btn-secondary.large {
    padding: 1.2rem 2.5rem;
    font-size: 1.1rem;
  }

  /* Container */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  /* Features Section */
  .features {
    background: var(--color-features);
    padding: 5rem 0;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-title {
    font-size: 2.5rem;
    color: var(--heading-color);
    margin-bottom: 1rem;
    font-weight: 700;
  }

  .section-subtitle {
    font-size: 1.2rem;
    color: var(--text-color);
    max-width: 600px;
    margin: 0 auto;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
  }

  .feature-card {
    background: var(--color-card);
    padding: 2rem;
    border-radius: var(--border-radius);
    text-align: center;
    transition: var(--transition);
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
  }

  .feature-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
  }

  .feature-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
    color: var(--primary);
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .feature-icon svg {
    width: 48px;
    height: 48px;
    color: var(--primary);
  }

  .feature-card h3 {
    color: var(--heading-color);
    margin-bottom: 1rem;
    font-size: 1.3rem;
  }

  .feature-card p {
    color: var(--text-color);
    line-height: 1.6;
  }

  /* Benefits Section */
  .benefits {
    background: var(--bg-main);
    padding: 5rem 0;
  }

  .benefits-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .benefits-text h2 {
    font-size: 2.5rem;
    color: var(--heading-color);
    margin-bottom: 1rem;
  }

  .benefits-text > p {
    color: var(--text-color);
    font-size: 1.2rem;
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .benefits-list {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .benefit-item {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
  }

  .benefit-icon {
    background: var(--primary);
    color: white;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.8rem;
    flex-shrink: 0;
    margin-top: 0.2rem;
  }

  .benefit-item h4 {
    color: var(--heading-color);
    margin-bottom: 0.5rem;
  }

  .benefit-item p {
    color: var(--text-color);
    line-height: 1.5;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }

  .stat-card {
    background: var(--color-card);
    padding: 2rem;
    border-radius: var(--border-radius);
    text-align: center;
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
  }

  .stat-card h3 {
    font-size: 2.5rem;
    color: var(--primary);
    margin-bottom: 0.5rem;
    font-weight: 700;
  }

  .stat-card p {
    color: var(--text-color);
    font-size: 0.9rem;
  }

  /* CTA Section */
  .cta-section {
    background: linear-gradient(135deg, var(--primary), var(--secondary-color));
    padding: 5rem 0;
    text-align: center;
  }

  .cta-content h2 {
    font-size: 2.5rem;
    color: white;
    margin-bottom: 1rem;
  }

  .cta-content p {
    font-size: 1.2rem;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 2rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .cta-section .btn-primary {
    background: white;
    color: var(--primary);
  }

  .cta-section .btn-primary:hover {
    background: rgba(255, 255, 255, 0.9);
    transform: translateY(-2px);
  }

  .cta-section .btn-secondary {
    background: transparent;
    color: white;
    border: 2px solid white;
  }

  .cta-section .btn-secondary:hover {
    background: white;
    color: var(--primary);
  }

  /* Animations */
  .fade-in {
    opacity: 0;
    transform: translateY(20px);
    animation: fadeIn 0.8s ease forwards;
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .delay-1 {
    animation-delay: 0.2s;
  }
  .delay-2 {
    animation-delay: 0.4s;
  }
  .delay-3 {
    animation-delay: 0.6s;
  }
  .delay-4 {
    animation-delay: 0.8s;
  }

  /* Responsive Design */
  @media (max-width: 992px) {
    .hero-content {
      flex-direction: column;
      text-align: center;
    }

    .benefits-content {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .features-grid {
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    }
  }

  @media (max-width: 768px) {
    .typing-text {
      font-size: 2.2rem;
    }

    .section-title {
      font-size: 2rem;
    }

    .hero p,
    .benefits-text > p,
    .cta-content p {
      font-size: 1rem;
    }

    .hero {
      padding: 2rem 1rem;
    }

    .features,
    .benefits,
    .cta-section {
      padding: 3rem 0;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }

    .btn-primary,
    .btn-secondary {
      width: 100%;
      max-width: 300px;
      text-align: center;
    }

    .stats-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  // Intersection Observer for animation triggers
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px',
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.style.animationPlayState = 'running';
      }
    });
  }, observerOptions);

  // Observe all fade-in elements
  document.addEventListener('DOMContentLoaded', () => {
    const fadeElements = document.querySelectorAll('.fade-in');
    fadeElements.forEach((el) => {
      el.style.animationPlayState = 'paused';
      observer.observe(el);
    });

    // Add staggered delay to feature cards
    const featureCards = document.querySelectorAll('.feature-card');
    featureCards.forEach((card, index) => {
      card.style.animationDelay = `${0.2 + index * 0.1}s`;
    });
  });

  // Smooth scrolling for anchor links
  document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start',
        });
      }
    });
  });
</script>
